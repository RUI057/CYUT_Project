# 訓練手語辨識模型（給第一次接觸的人）

## 你要做的事：3 步

### 1. 拿到專案
```bash
git clone <這個專案的網址>
```
訓練資料（`dynamic_dataset/`）已經在專案裡了，不用另外下載。

### 2. 開始訓練
| 系統 | 做法 |
|---|---|
| **Mac** | 雙擊 **`開始訓練.command`** |
| **Windows** | 雙擊 **`開始訓練.bat`** |
| 習慣用指令 | `python start_training.py` |

程式會自動：檢查 Python → 檢查/安裝套件 → 檢查資料 → 開始訓練。
過程中會問你兩個問題，直接按 Enter 用預設值就好。

### 3. 等它跑完
跑完會看到「訓練完成 ✅」，模型存在 `data/model.pkl`。

---

## 常見狀況

**Q: Mac 雙擊說「無法打開，因為來自未識別的開發者」**
在檔案上按右鍵 →「打開」→ 再按一次「打開」。或先在終端機執行一次：
```bash
chmod +x "開始訓練.command"
```

**Q: 說找不到 Python**
到 https://www.python.org/downloads/ 裝 Python 3.10 或 3.11。
Windows 安裝時**務必勾選 "Add Python to PATH"**。

**Q: 套件安裝失敗**
多半是系統 Python 被保護。改用虛擬環境：
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python start_training.py
```
（Windows 第二行改成 `.venv\Scripts\activate`）

**Q: 要跑多久？**
有 GPU（Mac M 系列 / NVIDIA）約 15 分鐘；純 CPU 可能 1 小時以上。
趕時間就在詢問時選 **快速模式**（跳過準確率評估，快很多）。

**Q: 出現一堆紅字錯誤**
把**完整**錯誤訊息截圖傳給組長，不要只傳最後一行。

---

## 訓練完可以做什麼

```bash
python test_model.py        # 用攝影機測試辨識
streamlit run app.py        # 開啟翻譯網頁
```

⚠️ 這兩個需要額外套件（mediapipe 等）：
```bash
pip install -r requirements.txt
```

---

## 進階選項

```bash
python start_training.py --quick          # 快速模式，跳過交叉驗證
python start_training.py --epochs 30      # 訓練輪數改 30（預設 60）
python start_training.py --yes            # 都不問，直接用預設跑
python start_training.py --out 測試.pkl    # 存到別的檔名，不覆蓋現有模型
```

---

## 看懂訓練結果

- **交叉驗證準確率**：模型真實實力的估計值，**這個才準**。
- **各詞樣本數**：低於 30 筆的詞會標「偏少」，那些詞會比較不準。
- **最常誤判的詞對**：哪兩個詞容易搞混，是之後補資料的優先目標。
