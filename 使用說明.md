# 手語辨識專案　使用說明（給第一次接觸的人）

## 開始使用：2 步

### 1. 拿到專案
```bash
git clone <這個專案的網址>
```
訓練資料已經在專案裡，不用另外下載。

### 2. 開起來
| 系統 | 做法 |
|---|---|
| **Mac** | 雙擊 **`開始.command`** |
| **Windows** | 雙擊 **`開始.bat`** |
| 習慣用指令 | `python start.py` |

會看到選單，照著按編號就好：

```
  1. 收集資料      用攝影機錄製手語動作
  2. 訓練模型      用收集到的資料訓練
  3. 測試辨識      用攝影機測試訓練好的模型
  4. 查看進度      看哪些詞收了、哪些還沒
  5. 檢查資料品質  找出錄壞的樣本
  0. 離開
```

第一次用某個功能時，程式會問要不要自動安裝需要的套件，按 Enter 就好。

---

## 收集資料怎麼操作

選 **1** 之後會先問要收哪個詞（可以直接按 Enter 從第一個沒收滿的開始），
然後開啟攝影機。

**流程**：手放進畫面 → 畫面倒數 3-2-1 → 比出手勢 → 自動存檔 → 換個角度再比一次

**按鍵**：

| 鍵 | 功能 |
|---|---|
| `N` / `M` | 下一個 / 上一個詞 |
| `J` | **跳到指定詞**（詞很多，用這個比較快）|
| `D` | 刪除剛剛那筆（比錯了就按）|
| `R` | 清空這個詞的全部資料 |
| `S` | 暫停 / 繼續 |
| `Q` | 離開 |

**收集要點**：
- 畫面上會顯示該詞的**比法提示**，照著比
- 每筆之間**換個距離或角度**，資料多樣模型才準
- 注意畫面右上有沒有顯示「臉：OK」——表情也是辨識的一部分
- 比錯了立刻按 `D` 刪掉，不要留著

---

## 常見狀況

**Q: Mac 雙擊說「無法打開，因為來自未識別的開發者」**
在檔案上按右鍵 →「打開」→ 再按一次「打開」。或先在終端機執行：
```bash
chmod +x "開始.command"
```

**Q: 攝影機打不開 / 畫面是黑的**
- 第一次執行時系統會問攝影機權限，要按「允許」
- Mac：系統設定 → 隱私權與安全性 → 攝影機 → 打開「終端機」
- 檢查有沒有別的程式（視訊軟體、瀏覽器）正在用攝影機

**Q: 說找不到 Python**
到 https://www.python.org/downloads/ 裝 Python 3.10 或 3.11。
Windows 安裝時**務必勾選 "Add Python to PATH"**。

**Q: 套件安裝失敗**
多半是系統 Python 被保護，改用虛擬環境：
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python start.py
```
（Windows 第二行改成 `.venv\Scripts\activate`）

**Q: 畫面中文變成方塊/亂碼**
系統缺中文字型。Windows 通常內建微軟正黑體；Linux 要另外裝：
```bash
sudo apt install fonts-noto-cjk
```

**Q: 訓練要跑多久？**
有 GPU（Mac M 系列 / NVIDIA）約 15 分鐘；純 CPU 可能 1 小時以上。
趕時間就選「快速訓練」。

**Q: 訓練時出現臉部資料分佈不均的警告**
這是**正常的提醒**，不是壞掉。因為舊資料沒有臉部資訊、新資料有，
模型可能會靠「有沒有臉」作弊。補收含臉樣本後就會消失。

**Q: 出現一堆紅字錯誤**
把**完整**錯誤訊息截圖傳給組長，不要只傳最後一行。

---

## 給想用指令的人

```bash
python collect_data.py                # 收集資料
python collect_data.py --word 開心     # 直接從「開心」開始收
python train_model.py                 # 完整訓練
python check_dataset.py               # 檢查資料品質
python test_model.py                  # 攝影機測試
streamlit run app.py                  # 翻譯網頁
```

---

## 看懂訓練結果

- **交叉驗證準確率**：模型真實實力的估計值，**這個才準**
- **各詞樣本數**：低於 30 筆的詞會標「偏少」，那些詞會比較不準
- **最常誤判的詞對**：哪兩個詞容易搞混，是之後補資料的優先目標
